# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'SimulateDeliveries', type: :request do
  describe 'POST /simulate_deliveries' do
    # it "works! (now write some real specs)" do
    #  get simulate_deliveries_path, params: {}, headers: { ACCEPT: 'application/vnd.api+json' }
    #  expect(response).to have_http_status(200)
    # end
  end
end
